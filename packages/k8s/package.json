{
  "name": "@devpod/k8s",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@devpod/types": "workspace:*",
    "@kubernetes/client-node": "^1.0.0"
  },
  "devDependencies": {
    "bun-types": "^1.1.0",
    "typescript": "^5.7.0"
  }
}
