# DevPod Dashboard Configuration
# Copy this file to .env and fill in your values

# Dashboard settings
DASHBOARD_PORT=3000
DASHBOARD_NAMESPACE=devpod
DASHBOARD_DEFAULT_IMAGE=mcr.microsoft.com/devcontainers/base:ubuntu
DASHBOARD_DISK_SIZE=50Gi
OPENVSCODE_PATH=/opt/openvscode-server

# Authentication
DASHBOARD_USER=admin
DASHBOARD_PASS=changeme
# DASHBOARD_USERS_FILE=/path/to/users.json

# Session secret (REQUIRED for production — generate with: openssl rand -base64 32)
SESSION_SECRET=

# GitHub token for private repo access (optional)
GH_TOKEN=

# Playwright E2E tests
TEST_BASE_URL=http://localhost:30080
TEST_USERNAME=admin
TEST_PASSWORD=changeme
